/**
 * Shared orbit angles for third-person camera
 * This allows MinecraftControls and PlayerCamera to share orbit state
 * without conflicts
 */

export const cameraOrbit = {
  y: 0, // Horizontal orbit angle
  x: 0, // Vertical orbit angle
}

